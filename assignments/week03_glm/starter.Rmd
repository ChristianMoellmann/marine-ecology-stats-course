---
title: "Assignment 1 — GLM"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(performance)
# library(DHARMa)
dat <- readr::read_csv("data/glm_presence.csv")
```

## 1. EDA
- Plot presence vs temp, salinity (e.g. jittered points, smooth).

## 2. Fit models
- Logistic GLM: presence ~ temp + salinity
- Check collinearity (`check_collinearity`), influence

```{r}
m1 <- glm(presence ~ temp + salinity, family = binomial(), data = dat)
summary(m1)
performance::check_collinearity(m1)
```

## 3. Diagnostics
- (If available) DHARMa simulated residuals
- ROC/AUC optional

## 4. Interpretation
- Odds ratios, 95% CI
- One paragraph ecological interpretation
```

